# Coding style

## PHP interfaces

Name every interface declared by this project with the `Interface` suffix.
Apply the convention to project code and tests; retain the published names of
interfaces supplied by dependencies.
